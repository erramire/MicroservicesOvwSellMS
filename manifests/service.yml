apiVersion: v1
kind: Service
metadata:
    name: erramiremicroservicesovwsellms
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: erramiremicroservicesovwsellms